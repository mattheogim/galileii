export async function runAuth(): Promise<void> {
  console.log("galileii auth — Google Calendar OAuth (Block 4 — coming next)");
  console.log("This will: open a loopback HTTP listener, redirect your browser");
  console.log("to Google's consent screen, and store the refresh token in ~/.galileii/.env");
}
